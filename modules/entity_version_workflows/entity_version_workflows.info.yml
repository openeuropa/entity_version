name: Entity version workflows
description: Entity version workflows extends core Worklows module by allowing to configure and control version numbers
  through state transitions.
type: module
core: 8.x

dependencies:
  - entity_version:entity_version
  - content_moderation:content_moderation
